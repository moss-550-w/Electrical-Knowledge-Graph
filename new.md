 一、核心体验质变（用户感知最强）

  1. 图谱叙事化入场动画 🎬

  现状：图谱加载后节点静态出现，毫无仪式感
  质变：节点按依赖深度分层、由内向外波纹式依次出现，金线节点最后以高亮爆发方式登场。这是用户第一印象，直接决定「这个网站
  不一样」的感受
  技术：ECharts animationDelay 按拓扑层级设置延迟 + 初始 symbolSize: 0 渐入

  2. 节点聚焦时的「知识上下文叙事」🧠

  现状：点击节点 → 右侧面板显示摘要，是纯信息展示
  质变：聚焦时图谱「收拢视野」——非路径节点淡出到几乎不可见，焦点节点及其直接关联节点放大形成局部特写，右侧面板有从节点位
  置飞入的动画过渡。用户感受到「镜头推进」而非「面板刷新」

  3. 路径生成的可视化演算过程 ⚡

  现状：点击「生成三条路径」→ 瞬间显示卡片
  质变：路径节点在图谱上逐步高亮点亮，模拟算法「搜索」的过程（每个节点间隔 80ms
  依次亮起），让用户看见知识被串联的过程，然后路径卡片从图谱侧边滑入

  ---
  二、内容深度质变（差异化核心）

  4. 「知识解剖」交互式公式推导 📐

  现状：KaTeX 渲染静态公式列表，没有上下文
  质变：每个公式旁增加可展开的「推导步骤」折叠块——比如 dq
  变换矩阵，可以展开看「为什么旋转矩阵是这个形式」的逐步推导。这是区别于任何现有学习平台的核心差异化功能
  技术：goldThread.json 增加 detail.steps[] 字段，Accordion 展开 + KaTeX 渐显

  5. 节点间「跨学科关联」惊喜时刻 🔗

  现状：关联节点是普通卡片列表
  质变：在详情页底部加一个「你可能没想到」区块——展示该知识点与远距离学科节点的隐性关联（比如「傅里叶分析」与「SVPWM」的
  联系），配一句解释性文字。这制造顿悟感，是知识图谱最能打动人的地方
  技术：graphStore 计算二度邻居，过滤掉同学科节点，取权重最高的 2-3 个

  ---
  三、视觉质变（观感档次提升）

  6. 深色主题 + 玻璃拟态 🌌

  现状：白色背景 + 标准 Element Plus 风格，没有辨识度
  质变：图谱背景改为深色星空（#0d1117），节点带内发光，顶栏、面板用 backdrop-filter: blur
  玻璃效果，整体视觉从「企业后台」升级为「科技感知识空间」
  代价小：只改 CSS，不动逻辑

  7. 图谱边的语义可视化升级 🕸️

  现状：不同关系类型用不同颜色线条，但差异不明显
  质变：depends_on 用带流动方向箭头的实线；maps_to 用虚线+对角纹理；suggest_sync
  用半透明波浪线。关系的形状语言直接传达语义，不需要图例

  ---
  四、功能质变（留存关键）

  8. 「我的学习地图」个人化路径记录 📍

  现状：用户探索的路径关闭页面就消失
  质变：localStorage 记录「已探索节点」，图谱上已访问节点有脚印标记，侧边栏有「我的探索历史」时间轴，下次回来图谱自动还
  原到上次视角。用户建立归属感和进度感

  9. 「为什么学这个」情境化引入 🎯

  现状：节点详情直接是技术内容
  质变：L3 节点详情顶部加一个「工程场景」卡片——比如打开「IGBT/MOSFET」时先显示「在一辆电动汽车里，你踩下油门的瞬间，这个
  器件每秒切换 2 万次…」，再展开技术内容。将抽象知识锚定到真实工程场景