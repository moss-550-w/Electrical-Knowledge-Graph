 关键事实（已勘察）

  - detail 加载已预留扩展位：contentStore.js:27 的降级路径 import('@/data/details/${nodeId}.json') ——
  补全骨架详情无需改任何代码，只要新建 src/data/details/{id}.json 即可被自动加载，且按需代码分割、不进首屏、不污染
  skeletonGraph.json。
  - 骨架节点 44 个（L1×11 + L2×33），全部仅有 summary，无 detail。
  - detail 完备结构：{ description, formulas[], formulas_steps[{formula,steps[{latex,note}]}], scene:{icon,text},
  anim:{type,src,caption} }。其中 formulas_steps（分步推导）+ anim（Lottie 动图）是金线 L3 的"完备"特征；动图需手写
  Lottie JSON，成本极高。
  - 主线机制：threads.js 的 THREADS 配置（apex + depends_on 传递闭包 + extraInclude − prune）+ TopbarControls.vue 的
  threads 按钮数组 + graphStore.activeThread（已支持任意 key）。新增主线无需改 store 逻辑。

  ---
  块 A：补全骨架节点详情

  载体：统一走 src/data/details/{id}.json 独立文件（零代码改动，自动生效）。

  成熟度梯度选择（核心决策）：

  ┌───────────┬─────────────────────────────────────────────────────┬────────────────┬────────────────┐
  │   档位    │                        内容                         │   单节点成本   │      适用      │
  ├───────────┼─────────────────────────────────────────────────────┼────────────────┼────────────────┤
  │ L2 半完备 │ description（200-400字）+ 2-4 核心公式 + scene 场景 │ 中             │ 44 个可批量产  │
  ├───────────┼─────────────────────────────────────────────────────┼────────────────┼────────────────┤
  │ L3 完备   │ 再加 formulas_steps 分步推导 +（可选）动图          │ 高（动图极高） │ 仅少数关键节点 │
  └───────────┴─────────────────────────────────────────────────────┴────────────────┴────────────────┘

  推荐：骨架统一补到 L2 半完备（description + formulas + scene，不加动图），把"完备 L3+动图"留给金线，维持 README 的
  L1/L2/L3 成熟度语义。分批推进：优先补三条现有主线途经的骨架节点（如
  s_grid_tied_inverter/s_buck_converter/s_transformer/s_state_space 等），而非一次性 44 个。

  ▎ 补全后这些节点 level 字段可由 L1→L2、L2→L2（内容达标即名副其实），是否升级 level 标签可一并定。

  ---
  块 B：新增工程主线

  候选 apex 对比：

  ┌────────────────┬─────────────┬────────────────────────┬────────────────────────────────────────────────────────┐
  │    候选主线    │ 与现有线重  │       可复用骨架       │                          评价                          │
  │                │     叠      │                        │                                                        │
  ├────────────────┼─────────────┼────────────────────────┼────────────────────────────────────────────────────────┤
  │                │             │ s_grid_tied_inverter / │                                                        │
  │ ☀️             │ 最低        │  s_boost_converter /   │ 推荐：叙事完整（光子→电网）、激活大量电力电子+电力系统 │
  │ 光伏并网发电   │             │ s_pfc / s_hvdc /       │ 骨架                                                   │
  │                │             │ s_rectifier            │                                                        │
  ├────────────────┼─────────────┼────────────────────────┼────────────────────────────────────────────────────────┤
  │ 电动汽车驱动   │ 高（PMSM/FO │ —                      │ 与 motor 线雷同，区分度低                              │
  │                │ C 重叠）    │                        │                                                        │
  ├────────────────┼─────────────┼────────────────────────┼────────────────────────────────────────────────────────┤
  │ 变频器/工业传  │ 高          │ s_rectifier            │ 同样与 motor 重叠                                      │
  │ 动             │             │                        │                                                        │
  ├────────────────┼─────────────┼────────────────────────┼────────────────────────────────────────────────────────┤
  │ 微电网/智能电  │ 中          │ s_ems / s_power_system │ 偏系统层，与现有深度内容衔接弱                         │
  │ 网             │             │ _structure             │                                                        │
  └────────────────┴─────────────┴────────────────────────┴────────────────────────────────────────────────────────┘

  推荐：光伏并网发电系统。新增工作量：
  1. 新建 apex 金线节点 pv_grid_system（L3 完整 detail）+ 配套节点：光伏电池 pv_cell、MPPT mppt、锁相环 pll、孤岛检测
  islanding_detection（L2/L3）
  2. threads.js 加 pv 配置（apex + extraInclude + prune）
  3. TopbarControls.vue 的 threads 数组加按钮 { key:'pv', label:'☀️ 光伏并网', type:'warning' }
  4. 可选 1-2 个新 Lottie 动图（MPPT 爬山、PLL 锁相）—— 成本高，可后置

  ---
  我的建议执行顺序

  1. 先做块 A 的第一批（三条现有主线途经的 ~12-15 个骨架节点补到 L2 半完备）—— 立竿见影、零代码风险、可验证流程
  2. 再做块 B 光伏并网主线（含新节点 + threads + 按钮）
  3. 动图按需最后补


后续（本轮不做）

     - 块A 剩余 31 个骨架节点详情（分批）
     - 块A 剩余 ~20 个骨架节点详情
     - 需把这些骨架节点的 level标签由内容达标后正式升级（L1→L2）
     - 块B ☀️ 光伏并网发电主线（新节点 + threads.js 配置 + TopbarControls 按钮）
     - 关键节点的 Lottie 动图